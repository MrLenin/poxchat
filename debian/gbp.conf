[DEFAULT]
debian-branch = debian/master
upstream-branch = upstream/latest
upstream-vcs-tag = v%(version)s
pristine-tar = True

[buildpackage]
sign-tags = True

[import-orig]
postimport = dch -v%(version)s-1 'New upstream release.'; git add debian/changelog; debcommit
